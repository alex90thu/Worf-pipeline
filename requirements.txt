# Python dependencies (pip)
# Note: fastp/minimap2/samtools/pysam are best installed via Conda (see environment.yml)

# Core scientific stack
matplotlib>=3.7
numpy>=1.24
pysam

# App server & database access
fastapi
uvicorn
pymongo
